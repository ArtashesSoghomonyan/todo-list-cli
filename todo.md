# To-do list for v0.1.0

* [ ] catch FileNotFound exception in the main.py
* [ ] create a validation system for .json sources,
    to make sure that it has items in it and every element is Todo (TypedDict)
* [ ] in removing items function make the index error user-friendly
